app-id: io.github.shundhammer.qdirstat
runtime: org.kde.Platform
runtime-version: 5.15-25.08
sdk: org.kde.Sdk
command: qdirstat
rename-desktop-file: qdirstat.desktop
rename-icon: qdirstat
finish-args:
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri
  - --filesystem=host

modules:
  - name: QDirStat
    buildsystem: qmake
    config-opts:
      - INSTALL_PREFIX=/app
    sources:
      - type: git
        url: https://github.com/shundhammer/qdirstat.git
        tag: '2.0'
        commit: 6415e6fa9196b92a4f55cf207376b5b48d3ad5ee
        x-checker-data:
          type: git
          tag-pattern: ^([\d.]+)$
      - type: patch
        path: appstream.patch
      - type: patch
        path: remove-unsandboxed-actions.patch
